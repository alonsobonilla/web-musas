import { objExport } from "./ordenar-producto.js"
// import { Ui } from "./Class/Ui.js";

document.addEventListener('DOMContentLoaded', () => {
    // const { nombre_producto, descripcion_producto, precio_producto, img_producto } = obj;
    console.log(objExport);
    // Ui.renderizarProducto(nombre_producto, descripcion_producto,precio_producto, img_producto);
})